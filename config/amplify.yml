version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - npm install
    build:
      commands:
        - ./scripts/build.sh
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
