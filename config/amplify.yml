version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22
        - npm install
    build:
      commands:
        - ./scripts/build.sh
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
